*.class
*.log
*.iml
*.jar
*.DS_Store
*.zip

# The Gradle wrapper jar must be committed — it's what lets `./gradlew` bootstrap
# on a fresh clone without a system Gradle install.
!gradle/wrapper/gradle-wrapper.jar


# temp
.cache
.history

# misc
.gradle/
.idea/
build/
.lib/
dist/*

# Kotlin/Gradle plugin metadata cache
.kotlin/

# Android SDK path — machine-specific
local.properties

# Kotlin/JS — node_modules is regenerated from kotlin-js-store/yarn.lock, which IS committed
node_modules/